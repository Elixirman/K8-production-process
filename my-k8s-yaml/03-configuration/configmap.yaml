# Non-sensitive settings, injected into the Deployment as env vars.
apiVersion: v1
kind: ConfigMap
metadata:
  name: my-app-config
  namespace: my-app
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
