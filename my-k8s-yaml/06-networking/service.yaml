# Gives the 3 Deployment replicas one stable internal address:
# my-app-service.my-app.svc.cluster.local
apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: my-app
spec:
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
